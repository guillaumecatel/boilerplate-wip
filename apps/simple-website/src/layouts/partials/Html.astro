---
import { getLanguageInfo } from '@myorg/shared/intl'

import { getLocale } from '@/i18n/runtime'

const locale = getLocale()
const { direction } = getLanguageInfo(locale)
---

<!doctype html>
<html
  lang={locale}
  dir={direction}
  class='antialiased'>
  <slot />
</html>
